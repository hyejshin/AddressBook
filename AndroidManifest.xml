<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hyejung.p1315842_10" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".List"></activity>
        <activity android:name=".RegisterForm"></activity>
        <activity android:name=".Register"></activity>
        <activity android:name=".UpdateForm"></activity>
        <activity android:name=".UpdateForm2"></activity>
        <activity android:name=".Update"></activity>
        <activity android:name=".DeleteForm"></activity>
        <activity android:name=".Delete"></activity>
    </application>

</manifest>
